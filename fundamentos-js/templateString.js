const nome = 'Rebeca'
const concatenacao = 'Olá ' + nome + '!'
const template = `
    Olá
    ${nome}!`
    console.log(concatenacao, template)

    //expressoes...
    console.log(`1 + 1 = ${1 + 1 }`)

    const up = texto => texto.toUpperCase()
    console.log(`Ei... ${up('cuidado')}!`) 
    
    /* acento de Crase.   `
    botão do acento agudo com shift e dando espaço
    criando uma constante chamada "up" que resultará
    em um ''texto'' para que seja Maiúscula */